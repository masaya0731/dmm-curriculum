<div class="container">
  <div class="row">
    <div class="mt-5 col-12"><b>Title:</b> <%= @post.title %></div>
    <div class="mt-3 col-12"><b>Body:</b> <%= @post.body %></div>
  </div>
  <div class="mt-3">
    <%= link_to "Edit", edit_review_path(@post), class: "review-btn" %>
    |
    <%= link_to "Back", reviews_path, class: "review-btn" %>
  </div>
</div>